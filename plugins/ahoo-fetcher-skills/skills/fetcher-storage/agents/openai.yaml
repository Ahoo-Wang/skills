interface:
  display_name: 'Fetcher Storage'
  short_description: 'Typed persisted values with cross-tab sync'
  default_prompt: 'Use $fetcher-storage to persist a typed value and keep it in sync across tabs.'
